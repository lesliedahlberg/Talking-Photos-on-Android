<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo">
        <item name="android:actionBarStyle">@style/MyActionBar</item>

        <item name="android:textColorPrimary">@color/icons</item>
        <item name="android:textColorSecondary">@color/icons</item>
        <item name="android:divider">@color/secondary_text</item>
    </style>

    <style name="MyActionBar"
        parent="@android:style/Widget.Holo.ActionBar">
        <item name="android:background">@color/primary</item>
        <item name="android:textColor">@color/icons</item>
    </style>


    <style name="recyclerViewFrame">
        <item name="android:background">@color/accent</item>
    </style>

    <style name="cardView">
        <item name="android:background">@color/primary_dark</item>
    </style>

    <style name="cardViewContentView">
    </style>

    <style name="cardViewContentViewImage">
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:tint">@color/primary_dark</item>
        <item name="android:tintMode">multiply</item>
    </style>

    <style name="cardViewMemContentViewImage">
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitXY</item>
    </style>

    <style name="cardViewDescription">
        <item name="android:background">@color/primary</item>
    </style>

    <style name="imageButton">
        <item name="android:background">#00ffffff</item>
    </style>

</resources>